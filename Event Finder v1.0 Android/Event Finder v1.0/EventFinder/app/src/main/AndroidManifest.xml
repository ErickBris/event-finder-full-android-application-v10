<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.eventfinder">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name="com.application.GlobalApplication"
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.projects.eventfinder.MainActivity"
            android:theme="@style/MainTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.SettingsActivity"
            android:theme="@style/AppTheme.SettingsTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/settings"/>

        <activity
            android:name="com.projects.activities.DetailActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.EventsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.NewsDetailActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.users.LoginActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.users.ProfileActivity"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.SearchActivity"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.SearchResultsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.CategorySearchActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.posts.PostsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.posts.NewPostActivity"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.events.MyEventsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.events.EventAddActivity"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.MapActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.events.EventEditActivity"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.activities.ImageViewerActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name="com.projects.eventfinder.WTActivity"
            android:theme="@style/SplashTheme"/>

        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

    </application>

</manifest>